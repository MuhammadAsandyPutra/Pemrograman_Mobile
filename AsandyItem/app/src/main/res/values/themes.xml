<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AsandyItem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorAccent">@color/my_light_accent</item>
        <item name="android:windowBackground">@drawable/app_background</item>

        <!-- Add more customizations as needed -->
    </style>

    <style name="Theme.AsandyItem" parent="Base.Theme.AsandyItem" />
</resources>
